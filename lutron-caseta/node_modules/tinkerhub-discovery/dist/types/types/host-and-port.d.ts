/**
 * Representation of a host and a port. Supports some basic parsing via
 * `fromString(input)` supporting values such as `example.com`, `example.com:80`
 * `10.0.0.1`, `10.0.0.1:80`, `2001:db8::1` or `[2001:db8::1]:80`.
 */
export declare class HostAndPort {
    readonly host: string;
    readonly port: number;
    constructor(host: string, port: number);
    readonly hasPort: boolean;
    /**
     * Create an instance from a string.
     *
     * @param input
     */
    static fromString(input: string): HostAndPort | null;
    static compare(a: HostAndPort, b: HostAndPort): number;
}
//# sourceMappingURL=host-and-port.d.ts.map