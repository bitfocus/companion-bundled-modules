import { BasicServiceDiscovery } from './internal';
import { Service } from '../service';
/**
 * Discovery implementation for manually adding or removing available services.
 */
export declare class ManualDiscovery<S extends Service> extends BasicServiceDiscovery<S> {
    constructor(type?: string);
    /**
     * Add a service that should be available.
     *
     * @param service
     */
    add(service: S): S | null;
    /**
     * Remove a service so that it is no longer available.
     *
     * @param service
     */
    remove(service: S | string): S | null;
    /**
     *
     * @param services
     */
    set(services: Iterable<S>): void;
}
//# sourceMappingURL=manual-service-discovery.d.ts.map