import { Service } from '../service';
import { ServiceDiscovery } from './service-discovery';
import { LayeredServiceDiscovery } from './internal';
import { AdvancedMapper } from '../mapper';
/**
 * Provides mapping of any discovery instance.
 */
export declare class MappedDiscovery<I extends Service, S extends Service> extends LayeredServiceDiscovery<S, I> {
    private readonly mappedServices;
    private readonly mapper;
    constructor(parent: ServiceDiscovery<I>, mapper: AdvancedMapper<I, S>);
    protected handleParentServiceAvailable(service: I): void;
    protected handleParentServiceUnavailable(service: I): void;
    protected handleParentServiceUpdate(service: I, previousService: I): void;
    protected updateService(service: S): S | null;
    protected removeService(service: S | string): S | null;
    private destroyService;
}
//# sourceMappingURL=mapped-service-discovery.d.ts.map