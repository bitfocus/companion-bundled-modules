import { Record } from '../manager';
/**
 * Helper for keeping track of when to send out a refresh query based on TTLs
 * of records.
 */
export declare class TTLRefreshHelper {
    private readonly query;
    private readonly records;
    private readonly listener;
    private readonly interval;
    constructor(query: () => void);
    destroy(): void;
    add(record: Record): void;
    remove(record: Record): void;
    private check;
    private scheduleRefresh;
}
//# sourceMappingURL=ttl-refresh-helper.d.ts.map