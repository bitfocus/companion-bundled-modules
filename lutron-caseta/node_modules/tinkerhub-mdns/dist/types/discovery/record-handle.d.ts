import { Record } from '../manager';
/**
 * Helper class for keeping a handle to a record.
 */
export declare class RecordHandle<T extends Record> {
    private readonly onSet;
    private readonly onRemove;
    private _record?;
    constructor(onSet: (record: T) => void, onRemove: (record: T) => void);
    readonly record: T | null;
    update(v: T | null): void;
}
//# sourceMappingURL=record-handle.d.ts.map